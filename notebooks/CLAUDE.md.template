# Thinkube Jupyter Environment

## Available Services

All services are accessible via `~/.thinkube_env` environment variables.

**Load environment:**
```python
from dotenv import load_dotenv
load_dotenv('/home/jovyan/.thinkube_env')
```

### Databases & Storage
- **PostgreSQL** - Relational database (`psycopg2-binary`)
- **Valkey** - In-memory store, Redis-compatible (`redis`)
- **ClickHouse** - Analytics database (`clickhouse-driver`)
- **SeaweedFS** - S3-compatible storage (`boto3`)

### Vector Databases
- **Qdrant** - Vector DB (`qdrant-client`)
- **Chroma** - Vector DB (`chromadb`)
- **Weaviate** - Vector DB (`weaviate-client`)

### Search & Analytics
- **OpenSearch** - Search engine (`opensearch-py`)

### AI/ML Tools
- **MLflow** - Experiment tracking (`mlflow`)
- **LiteLLM** - LLM gateway (`litellm`)
- **Langfuse** - LLM observability (`langfuse`)

### Data Annotation
- **Argilla** - Data labeling (`argilla`)
- **CVAT** - Computer vision annotation (web UI)

### Development Tools
- **JupyterHub** - Multi-user notebooks
- **code-server** - VS Code in browser
- **GitTea** - Git hosting
- **Harbor** - Container registry
- **DevPi** - PyPI mirror
- **ArgoCD** - GitOps CD
- **Argo Workflows** - Workflow engine

### Infrastructure
- **Keycloak** - Identity & SSO
- **JuiceFS** - Distributed filesystem
- **NATS** - Messaging system
- **Knative** - Serverless platform
- **Monitoring** - Prometheus/Grafana stack
- **pgAdmin** - PostgreSQL admin UI

## Directory Structure
```
~/thinkube/
├── notebooks/       # Your notebooks
├── examples-repo/   # Examples
└── shared/         # Shared resources
```

## User Notes

_Add your preferences and project context below:_

### Preferences
- [Your coding style, visualization preferences, etc.]

### Current Work
- [Active projects and context]
